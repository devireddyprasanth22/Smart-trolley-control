{
    "name": "website",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "build-esp": "tsc && vite build --outDir=../esp_comm/data --emptyOutDir",
        "preview": "vite preview"
    },
    "devDependencies": {
        "typescript": "^5.5.3",
        "vite": "^5.4.1"
    },
    "dependencies": {
        "rough-notation": "^0.5.1"
    }
}
